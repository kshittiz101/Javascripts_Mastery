// for of loop
// this loop is used to iterate over the value like arrays, strings and maps etc

// importants notes:
// Works on arrays, strings, sets, maps.
// Returns values directly (not keys/indexes).

// syntax
// for(let value of iterable){}

let arr1 = [55, 5, 52, 23];
for (let element of arr1) {
  console.log(element);
}
